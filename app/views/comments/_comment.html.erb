<%= div_for comment do %>
  <p>
    <strong><i>
      &emsp;Posted <%= time_ago_in_words(comment.created_at) %> ago, by <%= find_username_by_id(comment.user_id)  %>
    </i></strong>
    <br/>
    &emsp;<%= comment.content %>
  </p>
<% end %>